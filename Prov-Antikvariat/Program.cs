using System;

namespace Prov_Antikvariat
{
    class Program
    {
        static void Main(string[] args)
        {
            Items book1 = new Items();

            /* string fullAnswer;
            string answer;

            fullAnswer = Console.ReadLine();
            answer = fullAnswer.Trim();

            if ((String.Compare(answer, "book", true) == 0) || (String.Compare(answer, "bok", true) == 0))
            {
                
            } */
        }
    }
}
